C++ adapter (stub)

This folder contains a minimal example showing how a C++ service could format
logs to the Deepiri JSON schema. It's intentionally a stub and should be
expanded with a proper library and tests when needed.
